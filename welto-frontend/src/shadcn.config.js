const config = {
    theme: 'dark', // Choisissez 'light' ou 'dark'
    customComponents: {
      Button: {
        default: 'primary', // Définir le style par défaut pour les boutons
        variants: ['primary', 'secondary'], // Définir les variantes de style
      },
      // Ajouter d'autres composants ici...
    },
  };

export default config;
  